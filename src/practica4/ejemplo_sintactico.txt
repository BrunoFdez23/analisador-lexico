#include <stdio.h>
#include <stdiu.h>
#include <stdia.h>

int main(){
    int resultadoComplejoInt;
    resultadoComplejoInt = (10 + 20) * (30 - 15);
    resultadoComplejoInt == 450
    float resultadoComplejoFloat;
    resultadoComplejoFloat = (10.25 + 20.75) * (30.0 - 15.0);
    const int a = 10;
    int suma;        // Válido
    suma = (a + 5) - (2);
    float b;
    b = a;        // Error: Asignación de int a float
    c = 10;       // Error: Identificador no declarado
    const char* d = "Hola";
    //d = "Hola";
return 0;
}